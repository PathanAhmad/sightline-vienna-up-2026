# DECISIONS

One line per decision. Timestamped. No re-litigating.

- **2026-05-17** Rebrand: the project is **Sightline**, not "APG photo-QC." APG is no longer the named buyer; user-facing copy is product-led, with neutral "operator" framing for the customer. All UI, reports, PPT, and forward-looking docs updated. Older DECISIONS entries below still mention APG — that was the framing at the time and is preserved as history.
- **2026-05-17 (lesson, rebrand methodology)** Draft from the new brand's POV before editing the old artifact. First-pass deck rewrite kept the APG-buyer-sales arc (cost numbers, Pilot/KPIs/Scale CTA, Klosterneuburg refs) even with the strings replaced — fluent words, wrong frame. Substitution ≠ repositioning; on any future rebrand, start the new artifact blank, then decide which old sections survive.
- **2026-05-15** Going Challenge 2 (construction-photo QC).
- **2026-05-15** Stack: Python 3.11 + uv, Streamlit (no FastAPI for the spine), Claude Haiku 4.5 vision as the QC engine, classical CV (`imagehash` + ELA-via-Pillow) for forensics. See [pyproject.toml](pyproject.toml).
- **2026-05-15** No custom YOLO training. 30 hand-labels / 4 classes / 4 hours is below the practical small-data floor; VLMs win "is X present" questions; YOLO-World zero-shot is Plan B.
- **2026-05-15** No `imagededup`, `piexif`, `exifread`, `fastapi`, `uvicorn`, `easyocr`, `paddleocr` in the pre-pull.
- **2026-05-15** Differentiator: cross-photo authenticity / recycling detection. Demo line: "this photo was already submitted on job #X."
- **2026-05-15** HEIC handling stays defensive (`pillow-heif` opener + JPEG convert before any Claude upload), even though current data is JPEG.
- **2026-05-15** Architecture: hybrid (VLM + classical CV + forensics), not VLM-alone.
- **2026-05-15** Vision model: Claude Haiku 4.5 default; escalate to Sonnet 4.6 within Anthropic before cross-shopping.

### Late Friday — data drop & audit (`Resources/`)

- **2026-05-15 PM** Partner per the brief docx is **APG (Austrian Power Grid)**, not ÖGIG. Pitch to APG; the fiber data we received (Maria Rain, Carinthia, CLP20417A) is the working pilot stand-in.
- **2026-05-15 PM** Geomatch via **overlay-OCR** (Claude vision reads the photo's printed address + optional lat/lon + paper-label FCP code), not EXIF GPS. Sampled 200/200 photos have no EXIF GPS — WhatsApp upload path strips metadata.
- **2026-05-15 PM** CRS for the provided geo data is **WGS84 / EPSG:4326** (technically OGC:CRS84, equivalent). No Lambert 31287 reprojection needed for this dataset.
- **2026-05-15 PM** Six checks per the APG brief: warning tape, sand bedding, side view, depth reference, duplicate, GPS-consistency. Privacy stays flag-only (no pixel redaction).
- **2026-05-15 PM** `Beispiele/depth` + `Beispiele/duct` (+ 4 root exemplars: `bad`, `duct_sand`, `duct_depth`, `warnband`) loaded as **few-shot exemplars** in the Claude system prompt, not as training data.
- **2026-05-15 PM** Retract: hard-coded 30–40 cm trench depth in the prompt. That was an ÖGIG/fiber spec. Until Martin gives an APG number, treat the depth check as "is a depth reference visible / readable" — no threshold.
- **2026-05-15 PM** Retract: separate `04_research_log.md` / `RESEARCH.md` workflow. Past the long-form research phase — DECISIONS.md only from here. Reasoning lives in commit messages or chat.
- **2026-05-15 PM** Doc set cut to 4: `CLAUDE.md`, `README.md`, `PLAN.md`, `DECISIONS.md`. Old hackathon docs removed from working dir; git history keeps them.
- **2026-05-15 PM** Overlay-extraction approach **locked to Claude vision (Haiku 4.5)** — not Tesseract+regex. A 10-photo spread sample of `Resources/all/` shows variation that breaks fixed-crop / fixed-regex: overlay position varies (top-right, bottom-right, top-left depending on camera app); lat/lon appears in ≥3 formats (`46°33'56.226"N` DMS, `46.56153856N` decimal without separators, `Lat 46.551997, Long 14.294176` labeled); language mixes German + Cyrillic + English on identical fields (`Австрия` / `Austria`); ~30% of sampled photos show no lat/lon at all (address only). One vision call per photo absorbs all of this and also reads the in-frame paper FCP label when present.
- **2026-05-15 PM** Pipeline opens with a **"obvious-error" pre-filter** before per-segment classification: (a) `imagehash` perceptual-hash dedup runs first across the whole corpus (no API cost); (b) lat/lon ↔ printed-address consistency runs as a sanity flag on Claude's overlay output — forward-geocode the printed address, flag if the distance to the printed coords exceeds ~100 m. The geo check only applies to the subset of photos where both lat/lon and address are visible.

### Late Friday evening — eyes-on photo audit

- **2026-05-15 evening** Brief line `Kärntner Projekt ohne App` (Carinthia project w/o the GPS overlay app) is **wrong for *this* dataset**. Visual audit of 21 random photos across all naming patterns: 21/21 carry a burned-in overlay (date + address, often lat/lon, sometimes mini-map). EXIF audit of a 50-photo random sample: 0/50 have EXIF GPS. Overlay-OCR via Claude vision remains the right path. Do not rebuild around the brief's "ohne App" note.
- **2026-05-15 evening** Scoring granularity moves from **per-segment (2,983 LineStrings)** to **per (FCP, duct R-code) pair (~200 cells)**. The paper labels read `F### + R### + slot + color` — they encode FCP + duct main + position-in-bundle, never LineString segment ID. Median R-code spans 4 LineStrings; per-segment scoring was unsupported by the signals we can extract. FCP-level (9 cells) kept as drill-up only.
- **2026-05-15 evening** Add a **phase classifier** as a first-class output of the Claude vision call. Photos document different phases of work (excavation → depth-measure → duct-laid → sand-bedded → tape-laid → backfilled → restored, plus paper-label, staging, other). Each of the 6 QC checks is only scored on photos of its phase-relevant subset — a depth-measure photo missing warning tape no longer drags the duct's score down. Why: scene audit found dumpster shots, finished-asphalt shots, night flashlight shots, paper-label close-ups, etc.; treating all of them as "must score all 6" produced noise.
- **2026-05-15 evening** Add a **`relevance` gate** to the same call: `scorable / portrait / off_topic / unreadable`. Non-scorable photos are **hard-dropped** from per-duct scoring and surfaced separately on the report as "not classified" with reason. Hard-drop + soft-gate hybrid — cleanest demo, least unfair to contractors.
- **2026-05-15 evening** Soften the lat/lon-vs-address mismatch rule from `>100 m → flag` to `>150 m AND different street → flag`. Within-street disagreements between paper label and overlay are normal (paper = property being connected, overlay = photographer's standing position), not a fraud signal.
- **2026-05-15 evening** Off-cluster overlay addresses (e.g., `Lambichl`, neighboring village) → flagged as `geo_mismatch`, not silently dropped. SiteCluster polygon is the gate.
- **2026-05-15 evening** Confirmed overlay variability bigger than v1: 4 languages (DE/EN/Russian Cyrillic/transliterated), 4 lat/lon formats (incl. DMS-with-comma-decimal `46°33'29,30965"N`), 3 overlay positions (BR / TR / TL), ≥2 camera apps (TimePhoto family + GPS Map Camera). Overlay sometimes partially occluded by paper labels held against camera. Reinforces the vision-not-regex choice.
- **2026-05-15 evening** 43 files carry `— копия` suffix (Russian "copy") — pre-labeled duplicates baked into filenames. Showcase pair for the duplicate-detection demo segment.

### Late Friday evening — PPT reference-deck audit (the rubric I'd been missing)

- **2026-05-15 night** Reversal: granularity goes **back to per-LineString-segment** (2,983 cells) with a **photo-every-5m density rule**. The earlier FCP+duct pivot was wrong — it ignored the explicit rubric encoded in `Resources/oegig_ai_qc_presentation.pptx` slide 5: GREEN = "Compliant photo every 5 m + RTK GPS verified" / YELLOW = "Photos present but quality insufficient" / RED = "No compliant survey or photos." Per-segment positioning achieved via overlay lat/lon → nearest-LineString snap; paper labels are a *consistency check* (FCP + R-code should match the snapped segment), not the primary geomatch signal.
- **2026-05-15 night** Schema gains two new fields the brief didn't list but the ÖGIG deck slide 4 explicitly calls out: `pipe_ends_sealed` (white end-caps on the duct bundle) and `personal_data_visible` (faces / license plates — NIS2 compliance gate). Per-photo check count goes from 5 to 7. Per-photo cost unchanged (same vision call).
- **2026-05-15 night** Upgrade default vision model from Claude Haiku 4.5 to **Claude Sonnet 4.6**. Head-to-head on 5 hard cases (night flashlight, multi-phase priority, occluded overlay, comma-decimal lat/lon, previously-misread lat/lon): Sonnet wins 3/5, ties 1, slightly worse on 1. Full-batch cost ~$15 (Sonnet) vs ~$5 (Haiku). On a hackathon budget the $10 delta buys correctness on the demo's hardest cases — night photos no longer get dropped as `unreadable`, and the phase-priority rule (tape_laid beats depth_measure when both visible) actually fires.
- **2026-05-15 night** RTK GPS survey verification is part of the ÖGIG deck's compliance rubric but we have no RTK data — only ±3.79m overlay GPS. **Pitch framing:** "We assess the photo half of compliance; RTK survey is a complementary future phase." Do not silently pretend our coverage map equals full compliance.
- **2026-05-15 night** Nominatim address-to-coordinates lookup verified on 5 real Maria Rain addresses (4 land inside the SiteCluster polygon as expected; 1 Lambichl address correctly geocodes 1.7 km outside the cluster). Sub-second responses. Public-endpoint policy requires a UA with a real contact (use the project email, not "spike@example.com").
- **2026-05-15 night** Pitch outline rewritten to mirror the ÖGIG deck's narrative arc (hook → good-vs-bad visual → live map demo → 5-stage diagram → CTA), pruned to 5 slides. Borrows the deck's cost numbers (€120K per fiber cut, 3–5× multiplier, manual 3–5 days vs AI <30 min) — flagged to ask Martin tomorrow whether APG-specific numbers exist.
- **2026-05-15 night** v2 prompt verified consistent across the 30-photo random sample: 30/30 parse clean, all 4 v1 `off_topic` photos correctly upgrade to `scorable`, paper-label-as-portrait flip eliminated. Cache-read ratio ≥81%, full-batch cost projection unchanged at the Sonnet tier. Schema-driven structured output via `client.messages.parse()` with a Pydantic schema.

### Late Friday — final resources audit (the things I missed earlier)

- **2026-05-15 late** Filename `N_` prefixes are submission counters: same image stem under multiple prefixes = same photo re-submitted. **1027 prefixed files → 471 unique stems → 556 known duplicates baked into filenames**, plus 43 `— копия` (Russian "copy") files = ~600 ground-truth duplicates for free. Use this as recall validation for the pHash dedup pass: every multi-prefix family MUST land in the same pHash cluster, else our dedup is leaking. Also a natural demo set ("here are 12 photos a contractor submitted to 4 different jobs").
- **2026-05-15 late** Beispiele is a labeled subset of Fotos (114 + 105 files, 0 overlap, all also in Fotos). It's not separate data — it's a **calibration set**: after batch classification, measure phase-prediction accuracy on these 219 known-labeled photos. Gives us a pitch-able accuracy number ("we agree with the human labeler on N%").
- **2026-05-15 late** FCP polygon coverage analysis: union of 9 FCP polygons = 102.8% of SiteCluster (covers more area than the cluster itself), but with 18.7% gap inside the cluster and 21.5% spill outside. **Implication for geomatch:** point-in-polygon alone misses ~19% of addresses that fall in FCP gaps; need a `nearest_fcp_polygon` fallback. Spill outside the cluster is OK — those points still get an FCP assignment, just flagged as edge-of-zone.
- **2026-05-15 late** Slide 4 of `oegig_ai_qc_presentation.pptx` decoded by layout, not just text: left half = COMPLIANT example with 3 ✓ items (GPS, duct, sand); right half = NON-COMPLIANT example with 3 ✗ items (pipe ends sealed missing, ruler unreadable, persons/plates visible). Six photo-compliance criteria total. Combined with the 5 APG-brief checks (warning tape, sand, side view, depth, GPS-consistent + duplicate-detection in the forensics layer), we score against 8 photo-level checks.
- **2026-05-15 late** Smaller PPT media (2-13 KB PNGs) are decorations and the ÖGIG logo. No additional rubric content.
- **2026-05-15 Saturday AM** `segment_id` in the data contracts = `externalID` from Trenches.geojson (PLAN.md previously said `globalID` — that field doesn't exist in the file we got). Format: `SDIRouteSection_<digits>_<digits>`. Affects geomatch.csv + verdicts.csv consumers (app.py).
- **2026-05-15 Saturday** Reversal: photos with `personal_data_visible="yes"` are now **withheld at display time** in app.py (replaced with a GDPR notice card), not just flagged. Friday-night decision was "flag, don't pixel-redact" — correct for bookkeeping, but a demo that talks about NIS2 while rendering a worker's face on the projector is bad optics. Backend (verdicts, deficiency.csv, personal_data.csv) unchanged; only the photo grid in app.py swaps the image for a notice. One demo fixture (`fx_1_img_20240814_wa0046_jpg`, paper_label phase) carries the flag, so the GDPR moment is visible in the live demo.
- **2026-05-15 Saturday** Pre-emptive fix to friend's app.py line 201: it keyed segment lookup on `globalID` only; updated to try `externalID` first (real geojson), then `globalID`/`segment_id` (demo fixtures). Without this, every live-mode segment fell through to the default RED tint regardless of the verdicts.csv content.
- **2026-05-15 Saturday** Added `data/processed/audit.jsonl` — append-only failure/drop trail across all 5 stages. Helper in src/audit.py. Each stage logs `stage_start` / `stage_end` banners plus per-event lines (byte_identical_skip, phash_fail, api_fail, cost_ceiling_hit, drop_no_coords, off_cluster, latlon_address_mismatch, label_mismatch, drop_no_segment, drop_no_qc). Grep-friendly format; gitignored.
- **2026-05-17 Saturday AM** PHOTOS_DIR repointed from `data/Fotos/Fotos/` (770 hand-curated symlinks, lot 1 only) to `data/Resources/all/` (3,929 files → 3,227 unique by sha1). Reason: only ~9% of segments were getting any photo because the rest of the photo corpus was never ingested. Re-ingest + re-forensics → 3,223 phash representatives (2,500 new vs 723 already scored). Scoring split per cost: 500 new on Sonnet, remaining ~2,000 on Haiku. Existing 80 Sonnet + 643 Haiku results stay (resume by photo_id).
- **2026-05-17 Saturday AM** Hero KPI strip split single "% Compliant" into two top-line numbers: **Coverage** (segments documented / planned) and **Quality** (passing / documented). One ratio masked the two distinct failures (no photo taken vs photo failed spec) into one misleading number — on hyper-fragmented route plans (median segment 4 m), Coverage × Quality reads ~3% even when documented work is mostly fine. Audit cell split into per-model Sonnet / Haiku cells (time + photos + cost each) for cost-routing visibility.

### Open questions for Martin Fuhrmann (pitch/positioning — NOT technical blockers)

After re-reading the brief and the decks:
- NDA is on **route alignment** (`trassenverlauf`), not on photos. The brief says "you don't need real APG data to start; synthetic / publicly-available photos work fine for the prototype." Paid AI APIs with reasonable privacy policies are not "pastebins / public tools." Sending photos to Anthropic's API for processing is consistent with the brief. **Resolved: don't ask.**
- NIS2 personal-data filtering: AI flags `personal_data_visible=yes` and the report excludes those photos. Pixel-level pre-filtering before upload isn't feasible without a separate person-detector model — and the NIS2 concern is about flagging/auditing personal data, not about preventing AI from ever seeing it. **Resolved: don't ask, document the approach.**
- APG cost numbers: use the ÖGIG-deck figures as industry benchmarks with a footnote. **Resolved: don't ask.**
- Trench-depth spec: brief and deck both treat "depth reference visible" as the check, not a numeric threshold. **Resolved: don't ask.**
- RTK survey: not in the brief; explicitly framed as "future phase" in our pitch. **Resolved: don't ask.**

Remaining items, only worth asking if we see Martin in person:
1. **APG-only or APG + ÖGIG joint?** Brief says APG; data is fiber. Affects which logo and language we lead with.
2. **For demo: duplicate-photo-reuse pain vs missing-evidence pain?** Decides which demo move we open with.

Technical plan does not depend on either answer.
