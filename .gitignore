# Per-developer Claude settings
.claude/settings.local.json

# Playwright MCP cache/output
.playwright-mcp/

# Local screenshots and personal prep (not for team repo)
*.png
linkedin-profile-snapshot.md

# Partner-provided data (NDA on route info per the brief)
Resources/
data/
.audit_samples/
# Spike / batch outputs contain real addresses + lat/lon — same NDA
scripts/out/

# Local MCP config — contains API keys, never commit
.mcp.json
mcp.json

# Anthropic / partner keys live here, never commit
.env

# Python / uv
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Sphinx build output
docs/_build/

# MS Office lock files (auto-created when a .pptx/.docx is open)
~$*

# macOS Finder metadata
.DS_Store

# Stray Google Drive bulk downloads (we don't track these in git)
drive-download-*.zip
drive-download-*/
PPTS/

